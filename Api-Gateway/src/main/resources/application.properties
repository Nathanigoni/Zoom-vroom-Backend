server.port=${PORT:8080}
spring.application.name=api-gateway

# Service URLs (will be set as environment variables in Render)
user.service.url=${USER_SERVICE_URL:https://zoom-vroom-user-service.onrender.com}
ticket.service.url=${TICKET_SERVICE_URL:https://zoom-vroom-backend-ticket-service.onrender.com}
vehicle.service.url=${VEHICLE_SERVICE_URL:https://zoom-vroom-backend-vehicle-service.onrender.com}

# JWT Configuration
jwt.secret=${JWT_SECRET:your-jwt-secret-key}

# Logging
logging.level.com.example.Api=INFO